# PythonUtilites
Useful Python code snippets.

## [input](input)
A collection of classes which help verify that the input is of the type required without using 'exec'

## [JSON](json)
A collection of libraries which help read and write standard JSON as well has handle custom types.
Includes examples for each library as well as a complex example which uses all of the libraries to
handle a group of complex data structures.
